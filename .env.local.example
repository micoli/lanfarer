# Optional: override the default Bbox URL (useful if DNS doesn't resolve)
# BBOX_TARGET=https://mabbox.bytel.fr
# BBOX_HOST=mabbox.bytel.fr

# Optional: enable verbose request logging
# BBOX_VERBOSE=1
